services:
  listaecompra:
    image: postgres:16
    container_name: listaecompra
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_DB}
    ports:
      - '${DATABASE_PORT}:5432'

    volumes:
      - taskflow_data:/var/lib/postgresql/data

volumes:
  taskflow_data:
